import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.spear.gwageo",
  appName: "\uacfc\uac70\ud0d0\uc0c9\uae30",
  webDir: "public",
  server: {
    url: "https://spear.pics",
    cleartext: false,
  },
};

export default config;
